server:
  port: 8080

spring:
  application:
    name: spring-lock-transaction
  datasource:
    url: jdbc:postgresql://localhost:5444/lock_transactions
    username: root
    password: admin123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true


logging:
  level:
    org.hibernate.orm.jdbc.bind: trace

